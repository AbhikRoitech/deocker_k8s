# Copy to .env and fill in your database values. .env is git-ignored.

# Server
PORT=4000

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=appdb
PGUSER=app_admin
PGPASSWORD=change_me

# Set to "true" when connecting to AWS RDS (TLS is enforced by the DB).
PGSSL=false
